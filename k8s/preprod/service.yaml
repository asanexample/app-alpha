apiVersion: v1
kind: Service
metadata:
  name: app-alpha
  namespace: team-alpha
spec:
  type: ClusterIP
  selector:
    app: app-alpha
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
