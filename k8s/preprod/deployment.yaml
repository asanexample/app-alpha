apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-alpha
  namespace: team-alpha
  labels:
    app: app-alpha
    app.kubernetes.io/name: app-alpha
    team: alpha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-alpha
  template:
    metadata:
      labels:
        app: app-alpha
        app.kubernetes.io/name: app-alpha
        team: alpha
    spec:
      # Pod Identity binds AWS creds to this named SA (platform association, ADR-041).
      serviceAccountName: app-alpha
      # App does not talk to the Kubernetes API — drop the SA token (least privilege). Pod Identity
      # injects its OWN projected token, so this does not affect AWS access.
      # Required by the platform Kyverno policy restrict-automount-sa-token.
      automountServiceAccountToken: false
      containers:
        - name: app-alpha
          image: 829808296602.dkr.ecr.us-east-1.amazonaws.com/team-alpha/demo@sha256:b3e83901c96a4787a1148c7820e84628347e40e5bab04a7f4a2dcb2a46a8a392
          ports:
            - containerPort: 8080
          env:
            - name: VERSION
              value: "7df8217b5e0a4da1829297dd4a92a988bedff30d"
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            # Team S3 bucket (read via Pod Identity). AWS_REGION lets the SDK pick the endpoint; creds
            # come from the Pod Identity agent, not this config.
            - name: DATA_BUCKET
              value: "asanexample-team-alpha-data"
            - name: AWS_REGION
              value: "us-east-1"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
