apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-alpha
  namespace: team-alpha
  labels:
    app: app-alpha
    app.kubernetes.io/name: app-alpha
    team: alpha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-alpha
  template:
    metadata:
      labels:
        app: app-alpha
        app.kubernetes.io/name: app-alpha
        team: alpha
    spec:
      # Pod Identity binds AWS creds to this named SA (platform association, ADR-041).
      serviceAccountName: app-alpha
      # App does not talk to the Kubernetes API — drop the SA token (least privilege). Pod Identity
      # injects its OWN projected token, so this does not affect AWS access.
      # Required by the platform Kyverno policy restrict-automount-sa-token.
      automountServiceAccountToken: false
      containers:
        - name: app-alpha
          image: 829808296602.dkr.ecr.us-east-1.amazonaws.com/team-alpha/demo@sha256:f17ef3b286f630e973915aca893389d79f7156c9622b39bde3bf298c110605c1
          ports:
            - containerPort: 8080
          env:
            - name: VERSION
              value: "8b67cf43de4bb372b24fc1cf892ddf4709c24b5d"
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            # Team S3 bucket (read via Pod Identity). AWS_REGION lets the SDK pick the endpoint; creds
            # come from the Pod Identity agent, not this config.
            - name: DATA_BUCKET
              value: "asanexample-team-alpha-data"
            - name: AWS_REGION
              value: "us-east-1"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
