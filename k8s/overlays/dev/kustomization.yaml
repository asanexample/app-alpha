# dev (the default stage). Single replica; the image digest is pinned by CI on each promote-by-PR (#377) —
# the newTag line below is the seam CI rewrites to the signed digest.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
replicas:
  - name: app-alpha
    count: 1
images:
  - name: 829808296602.dkr.ecr.us-east-1.amazonaws.com/team-alpha/demo-web
    digest: sha256:5ab618fe03e22450f709f63e6e27c66d6aa0e94328ae2032c088c230b35328c7
